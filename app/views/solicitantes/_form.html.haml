= form_for @solicitante do |f|
  -if @solicitante.errors.any?
    #error_explanation
      %h2= "#{pluralize(@solicitante.errors.count, "error")} prohibited this solicitante from being saved:"
      %ul
        - @solicitante.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :nome
    = f.text_field :nome

  .actions
    = f.submit 'Save'

